TODO:

vexiterror is a way to exit the program with an error message.  It is used
in all programs except the following along with function vauth_open() that 
tests for access to the database.  It should probably be added to the following
programs too:

vchkpw
vdelivermail
vkill
vpopmaild

